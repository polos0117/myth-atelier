# 그림 규칙

무기당 **액션 한 장**이다. 폼도 기준 시트도 없다. 그 한 장이 곧 그 캐릭터다.
세 상태가 있다 — 기본(정체성) · 각성(권능의 극대화, 3성) · 저주(전승의 어두운 면, 싸움 중 발현). 각성과 저주는
액션 그림을 첨부해 같은 구도에서 만든다. 둘 다 선택이라 없으면 기본 그림이 뜬다.

## 파일 이름

```
<카드>_<화풍>_f.webp             액션 한 장 — 카드 그림. 필수
<카드>_<화풍>_f_awaken.webp      각성 — 3성일 때 대신 뜬다. 선택
<카드>_<화풍>_f_cursed.webp      저주 — 저주가 발현되면 대신 뜬다. 선택
<카드>_<화풍>_f_casualN.webp     일상컷 N. 선택
<카드>_<화풍>_f_extraN.webp      특별컷 N — 저주형처럼 놀이에 안 쓰는 변주. 선택
<카드>_<화풍>_f_skin_<열쇠>.webp         스킨 — 정식 모습 위에 입히는 치장. 선택
<카드>_<화풍>_f_skin_<열쇠>_awaken.webp  그 스킨의 각성. 선택
<카드>_<화풍>_f_skin_<열쇠>_cursed.webp  그 스킨의 저주. 선택
```

- `<카드>` 는 `data/card.json` 의 `name`(한국어 정본). 이미지 생성 후 파일 이름을 만들 때 무기 이름의 모든 공백을 반드시 밑줄(`_`)로 바꾼다. 예: `아킬레우스의 창` → `아킬레우스의_창_game_keyart_f.webp`. 이 규칙은 기본·각성·저주·일상·특별컷·스킨에 똑같이 적용한다.
- `<화풍>` 은 `data/style.json` 의 key. 생성기(`prompt.html`)가 파일 이름을 같이 찍어 준다.
- `_f` 는 반드시 붙인다. 지금은 여성만 만들지만 남성을 더해도 옛 파일의 뜻이 안 바뀐다.
- 스킨 열쇠는 `data/card.json` 의 그 카드 `skins` 에 먼저 적는다(`{key, name, look}`, 열쇠는 영문 소문자·숫자). 없는 열쇠는 등록기가 안 받는다.
  스킨 기본은 액션 그림을 첨부해 같은 구도에서 장갑·색만 바꾼다. 스킨의 각성·저주는 **그 스킨 기본**을 첨부해 만든다(선택 —
  없으면 판에서 화면이 스킨 기본 위에 효과를 덮는다).
  다른 작품에서 온 발상이어도 열쇠·이름·묘사에 그 작품의 고유 이름은 쓰지 않는다.
- 크기는 세로 2:3, **1024×1536**, webp. 액션과 각성은 같은 자리에 번갈아 뜨므로 크기가 같아야 한다.

## 조건

한 명. 온몸이 프레임 안. **무기를 모티브로 한 메카 의인화** — 무기를 들지 않는다. 무기의 재질·색·형태·문양(카드의 `look`)이
판·관절·바인더·발광선에 옮겨 가 있고 손은 손이다. 얼굴이 카메라를 향한다(뒷모습 아님).
글자·로고·테두리 없음 — 책이 카드인 이형(훈민정음·손자병법 같은 것)도 펼친 쪽·문장·제목 없이 문양과 상징으로만 그린다.
훈민정음만 낱자(ㄱ·ㅁ·ㅇ·ㅅ…)를 문양으로 띄우는 것을 예외로 둔다. 생성기는 가짜 글자를 쓰므로 문장은 늘 틀린다. 각성은 액션 그림을 첨부하고 같은 구도에서 무기만 깨운다. 저주는 같은 구도에서 룬과 빛이
오염되고 깃털·천·후광이 갈라진다 — 은색 장갑은 그대로.

## 순서

1. `prompt.html` 에서 무기·화풍·출력을 고르고 문장을 복사한다. 파일 이름도 거기 있다.
2. 그림 모델에 넣어 만든다. 각성은 액션 그림을 첨부한다.
3. webp 로 바꿔(1024×1536) 그림 저장소 `myth-atelier-img/img/` 에 올린다. 카드 하나에 커밋 하나.
4. 올리면 자동이다. 그림 저장소의 썸네일 워크플로가 돌고, 코드 저장소를 깨워 `data/img.json` 에 적는다.
   안 뜨면 코드 저장소 Actions 에서 "그림 등록" 을 수동 실행한다.
5. 도감(`dex.html`)에 뜨면 끝. 등록 실패는 Actions 요약에 파일 이름과 이유가 남는다.

## 검사

```bash
ln -s ../myth-atelier-img/img img            # 그림 저장소를 옆에 받았을 때
python3 tools/register-images.py --check    # 이름이 맞는지, 무엇이 새로 들어가는지
node tests/data.cjs                         # 자료가 서로 맞는지
```

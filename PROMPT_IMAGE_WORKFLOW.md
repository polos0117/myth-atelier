# prompt.html 이미지 생성·업로드 작업 규칙

이 문서는 `polos0117/myth-atelier`의 `prompt.html`로 문장을 뽑아 그림을 만들고, 그림 저장소(`myth-atelier-img`)와 도감에 반영하는 작업 절차다. 이전 세션의 완료 기록이 아니라, 다음 세션에서도 재사용할 작업 규칙이다.

작업 전 [IMAGE_RULES.md](IMAGE_RULES.md)를 함께 읽는다. 파일 이름·화풍·크기·두 저장소 순서의 상세 규칙은 그 문서를 따른다. 올리는 손은 `tools/upload-art.py`와 `.claude/skills/upload-art/`가 한다. 현재 사용자의 카드·수량·화풍·연출 지시가 이 문서의 기본 작업 구성보다 우선한다.

## 1. 전체 순서

**카드·기존 그림 확인 → prompt.html 설정·추가 지시 → 최종 프롬프트 1회 확인 → 기준컷 생성·검수 → 각성·저주·일상컷 파생 → WebP 변환·구도 대조 → 그림 저장소 `main`에 카드 하나 커밋 하나 → 자동 썸네일·`data/img.json` 등록 확인 → 완료**

사용자가 이 전체 작업을 요청했다면 그림 생성만 끝내고 완료라고 말하지 않는다. 단계별 실제 상태를 기록하고 요청된 범위까지 진행한다.

## 2. prompt.html을 기준으로 생성한다

- 최신 `main`의 `prompt.html`을 사용한다. 배포 화면을 쓸 경우 작업하려는 판과 같은지 확인한다.
- 브라우저 조작이 가능한 환경에서는 실제 생성기를 조작한다. 카드 선택을 적용하고 화풍·디자인 언어·출력(기본·각성·저주·일상)·외형 설정을 확인한 뒤 출력 프롬프트를 확보한다.
- 기억에 의존해 비슷한 프롬프트를 새로 쓰고 `prompt.html`을 사용했다고 보고하지 않는다. 화면 접근이 안 되면 그 사실을 밝히고, 같은 코드의 로컬 실행(`lib/prompt-myth.js`를 node로 돌리는 것도 된다)으로 정확한 출력을 확보한다.
- 실제 생성에 제출한 프롬프트를 보존한다. 랜덤 선택이 적용된 경우 설정값만으로 같은 출력을 재구성할 수 있다고 가정하지 않는다.
- 사용자가 요청한 의상·분위기·구도·인물 조건을 임의로 완화하거나 다른 창작 방향으로 바꾸지 않는다. 적용되는 안전 규칙과 도구 제한을 따르며, 실패 시 원인을 설명하고 가능한 범위의 최소 변경을 명시한다.
- 과거 브라우저 선택값은 과거 그림의 화풍 증거가 아니다. 이번 생성에 실제 사용한 화풍 키와 프롬프트를 함께 기록한다.

### 생성기 원문 우선과 A/B 최소 보정

1. 생성기가 만든 실제 최종 원문을 **A안**으로 삼아 먼저 그대로 제출한다. 처음부터 기억이나 수작업 문장으로 대체하지 않는다.
2. 생성 결과를 확인해 안전 차단, 필수 선택값 누락, 해부 오류, 별도 무기 생성처럼 명확한 실패가 있을 때만 **B안**을 만든다. 단순한 취향 차이나 더 좋아질 것 같다는 이유만으로 다시 뽑지 않는다.
3. B안은 실패 원인에 직접 대응하는 문장만 추가하거나 교체한다. 문제가 없던 카메라·구도·노출 정도·의상·장면·표정·색은 함께 바꾸지 않는다.
4. 예를 들어 빈손인 기준컷의 파생 그림에 새 검이나 별도 무기가 생겼다면, 기준 그림에는 별도 무기가 없으므로 추가하지 말고 양손을 기준과 같이 비우라는 제한만 덧붙인다. 손이 창이 되어 버렸다면(전에 실제로 그랬다) 손은 손이라는 해부 제한만 덧붙인다.
5. A안과 B안, 재생성 사유, 최종 채택 결과를 구분해 남긴다. B안만 남겨 A안이 실제 생성기 출력이었던 것처럼 기록하지 않는다.

### 선택 항목 우선·텍스트 보기 검토

1. 카드·화풍·디자인 언어·출력과 나이·인종·얼굴형·머리·체형·눈색·머리색 등 생성기가 제공하는 항목은 가능한 한 실제 선택 UI에서 먼저 설정한다. 외형 전체를 추가 지시에 몰아 쓰지 않는다.
2. 선택지에 없거나 출력만으로 부족한 내용만 **추가 지시**로 보완한다. 선택값으로 표현할 수 있는 내용은 해당 항목에서 수정하고, 중복하거나 반대 조건을 덧붙이지 않는다.
3. 설정과 추가 지시를 반영한 **실제 제출 최종본을 출력 칸에서 그림별로 1회 확인**한다. 화풍·출력·외형과 누락·충돌을 함께 확인하고, 확인한 원문을 생성에 사용한다. 항목별로 별도 검토 단계를 반복하지 않는다.
4. 확인 후 설정이나 원문을 수정한 경우에만 변경된 최종본을 다시 확인한다. 변경 없는 원문은 재검토하지 않는다.
5. 무기별 저장(생성기의 저장 단추)으로 선택값을 남긴다. 과거 텍스트 지시를 나중에 선택값으로 바꿔 당시 기록처럼 남기지 않는다.

기본값으로 둔 항목(디자인 언어 메카, 선택 문단 끔, 동작 자동)은 사용자가 달리 말하지 않는 한 그대로 둔다. 선택 우선 원칙은 모든 항목을 강제로 고정하거나 화풍 간 동일하게 맞추라는 뜻이 아니다.

### 나이 선택과 얼굴 다양성

젊은 성인이 전설에 휘말리는 분위기를 기본으로 삼는다. 나이를 별도로 지정하지 않은 새 인물은 `youthful adult`부터 `30s` 범위에서 정하되, **`youthful adult`와 `20s`를 우선**한다.

| 나이 설정 | 선택 기준 |
|---|---|
| `youthful adult` · `20s` | 기본으로 가장 자주 사용한다. `youthful adult`는 동안인 성인을 뜻한다. |
| `30s` | 무기의 전설에 성숙함이나 노련함이 어울릴 때 선택한다. |
| `40s` 이상 · 연륜 있는 `mature adult` | 무기 콘셉트나 원래 주인의 성향에 뚜렷한 근거가 있을 때만 예외적으로 사용하고, 그 이유를 기록에 남긴다. |

- 얼굴 다양성을 확보하려고 나이를 반복해서 올리지 않는다. 얼굴형·눈매·코·입·헤어·표정 등으로 인물 차이를 만든다.
- 원래 주인(`card.json`의 `wielder`)의 성향은 나이와 분위기를 판단할 때 참고할 수 있다. 주인의 외모를 자동으로 닮게 하지 않는다.
- 실제 생성기의 나이 선택에 반영하고, 추가 지시가 선택한 나이 인상을 불필요하게 높이지 않는지 출력 칸에서 확인한다.
- 이 기준은 이후 새 인물 생성에 적용한다. 기존 그림의 나이를 소급 변경하지 않으며, 같은 인물의 화풍 비교·일상컷에서는 확정된 성인 나이를 유지한다.

## 3. 카드별 그림 생성의 기본 구성

별도 구성을 지정하지 않았다면, 카드마다 **게임 키아트 2.5D (`game_keyart`) 화풍으로 총 4장**을 다음 순서로 생성한다.

| 순서 | 출력 | 생성물 | 파일 |
|---|---|---|---|
| 1 | 기본 (portrait) | 여성 메카 의인화 전신 1장 — 기준컷 | `<카드>_game_keyart_f.webp` |
| 2 | 각성 (awaken) | 기준컷을 첨부해 같은 구도에서 권능만 깨운 1장 | `…_f_awaken.webp` |
| 3 | 저주 (cursed) | 기준컷을 첨부해 같은 구도에서 오염만 더한 1장 | `…_f_cursed.webp` |
| 4 | 일상 (casual) | 1번 인물의 일상 단일컷 1장 | `…_f_casual1.webp` |

- 남성 의인화는 만들지 않는다. 추가 화풍과 둘째 일상컷은 기본 구성에 포함하지 않는다.
- 대상 카드와 수량은 현재 요청에 따른다. 과거 카드 목록이나 작업 수량을 고정값으로 삼지 않는다.
- 사용자가 추가 화풍 비교를 명시한 경우에만 해당 구성을 적용하며, 같은 인물의 화풍 비교에서는 인물 정체성을 유지한다. 서로 다른 카드까지 같은 얼굴로 통일하지 않는다.

## 4. 포즈·시점·일상컷 규칙

| 항목 | 규칙 |
|---|---|
| 기준컷 | 동작(`ACTIONS`)은 자동. 얼굴이 카메라를 향한 전신, 손은 손, 무기를 들지 않는다 |
| 추가 화풍의 기준컷 (별도 요청 시) | 동작은 동일하게 자동. 첫 그림의 세부 자세·구도·연출을 재현할 필요 없음 |
| 추가 화풍 비교 시 유지할 것 | 같은 인물의 정체성과 해당 카드의 `look` |
| 기준컷의 나머지 연출 | 세부 자세·팔다리 배치·장비 강조 등은 컷마다 달라도 됨 |
| 일상컷 | 기준컷과 다른 일상 장면으로 구성. 원본 구도·화각에 맞추지 않고 장면에 맞게 자유롭게 연출. 현재 요청의 제한은 준수 |
| 일상컷 참조 | 기준컷을 실제 참조로 사용해 같은 인물 유지 |
| 스킨 | 생성기 출력 "스킨". 기본은 기준컷을 첨부해 같은 인물·같은 구도에서 장갑 재질·색·문양만 스킨 `look` 으로. "스킨 상태" 각성·저주는 그 스킨 기본을 첨부해 만든다(선택) |

"자동"은 생성기 선택값을 뜻하며, 출력 포즈를 똑같이 맞추라는 뜻이 아니다. 참조 그림은 인물과 무기 정체성을 유지하는 기준으로 사용하며, 세부 자세까지 복제하도록 지시하지 않는다.

일상컷은 기준컷을 배경만 바꾼 복제 컷으로 만들지 않는다. 일상 출력의 프롬프트를 사용하고, 기본 출력의 장비나 전투 지시를 임의로 덧붙이지 않는다.

### 기준컷에서 각성·저주·일상컷을 파생하는 세트

1. **기준컷을 먼저 확정한다.** 카드, 화풍, 출력 비율(2:3)을 생성기에서 설정한다. 기준컷의 선택 옵션은 콘셉트와 요청 구도를 해치지 않는 범위에서 정할 수 있다. 사용자가 "기본값만" 또는 특정 옵션 고정을 지시한 대상은 임의 선택하지 않는다.
2. **기준컷을 검수 관문으로 삼는다.** 한 명의 성인 인물인지, 요청된 전신·방향·프레이밍인지, 얼굴과 손·팔다리가 자연스러운지, 별도로 들거나 떨어진 무기가 생기지 않았는지, 재질·문양·색과 실루엣이 카드의 `look`을 반영하는지 확인한다. 무기 의인화는 원전 무기를 손에 든 소품으로 복제하지 않고 갑옷·의상·장식·빛의 구조로 통합한다. 떠 있는 효과는 손에 든 별도 무기가 아니라 효과 또는 신체·장비에 붙은 구조로 명확히 읽혀야 한다.
3. **기준컷만 파생 그림의 인물·구도 기준으로 사용한다.** 각성과 저주는 기준컷을 실제 참조 그림으로 첨부하고, 카메라·포즈·프레이밍·피사체 크기·얼굴·헤어·기본 의상과 색을 유지한다.
4. **각성은 힘의 발현만 바꾼다.** 신화적 광휘, 룬·문양의 활성화, 에너지와 주변 환경의 반응만 강화한다. 기준컷에 없던 별도 무기, 새 갑옷 구조, 다른 포즈를 추가하지 않는다.
5. **저주는 오염 상태만 바꾼다.** 기본 갑옷·재질·색은 유지하고 적색·자주색 계열의 오염광, 갈라짐과 새어 나오는 빛, 해진 천·깨진 후광, 그에 맞는 눈과 표정만 변화시킨다. 카드의 `curse.text`가 방향이다. 기준컷에 없던 별도 무기를 추가하지 않는다.
6. **일상컷은 생성기의 일상 출력을 사용한다.** 전체 랜덤은 한 번만 실행하고 안전 차단이나 필수 선택값 위반이 없는 한 마음에 드는 항목이 나올 때까지 다시 돌리지 않는다. 기준컷은 같은 인물의 얼굴·헤어·성인 나이·체형을 유지하는 참조로만 쓰며, 평상복으로 바꾸고 갑옷과 손에 든 무기는 제거한다. 무기 정체성은 작은 액세서리·문양·색의 반향으로만 남긴다.
7. **일상컷의 연출은 선택 결과를 따른다.** 장면·카메라·크롭·포즈는 기준컷과 달라도 된다. 안전 차단이 노출·분위기 표현에서 발생하면 선택한 장소·행동·방향은 유지하고 해당 표현만 중립적이고 단정하게 바꾼다. 랜덤으로 확정된 필수 방향이나 구도가 무시되면 그 항목만 B안으로 강화한다.
8. **화풍은 선택명이 아니라 실제 결과로 검수한다.** `게임 키아트 2.5D`는 평면 애니 일러스트가 아니라 입체적인 얼굴·재질·조명과 키아트 렌더링이 읽혀야 한다. 결이 다르게 나오면(전에 미스틸테인이 파스텔로 나왔다) 화풍 문장을 고치지 않고 다시 뽑거나 맞는 화풍으로 옮긴다 — 문장을 고치면 이미 만든 그림과 틀어진다. 사용자가 결과를 우선 보류하라고 한 경우에는 기존 결과를 소급 변경하지 않고 다음 생성부터 적용한다.

### 일상컷의 구도·화각 자유 구성

- **일상컷은 기준컷의 구도·화각·카메라 거리·시점·피사체 크기·여백·포즈를 맞출 필요가 없다.** 장면과 행동에 어울리도록 새롭게 자유 구성한다.
- 기준컷 참조는 얼굴·헤어·피부색·눈 색·성인 나이·체형 등 같은 인물의 정체성을 유지하는 기준이다. 원본의 촬영 구도나 프레이밍을 고정하는 기준으로 사용하지 않는다.
- 기준컷의 구도 유지나 여백·카메라 거리 변경 금지 지시를 일상컷에 자동으로 이월하지 않는다. 일상컷 프롬프트와 추가 지시에도 원본 구도·화각·피사체 크기를 고정하는 문구를 관성적으로 넣지 않는다.
- 사용자가 해당 일상컷의 구도·화각·프레이밍을 명시한 경우에는 그 요청을 우선한다.

### 추가 화풍이나 둘째 일상컷을 요청한 경우

- 같은 카드의 두 일상컷은 서로 다른 장면으로 만든다. 인물 정체성은 유지하되 **장소·의상 실루엣·행동·포즈·구도**를 다르게 정하고, 카메라 거리·각도·조명·분위기도 장면에 맞게 변화시킨다. 색상이나 작은 소품만 바꾼 동일 장면은 다양성으로 보지 않는다(천총운검의 낮·밤 한 쌍이 좋은 예다).
- 둘째 일상컷을 설정할 때 첫 일상컷의 장면 지시를 그대로 재사용하지 않는다. 생성기 선택값을 먼저 바꾸고, 부족한 차이만 추가 지시로 보완한다.
- 업로드 전에 두 일상컷을 나란히 대조해 장면·의상·행동·구도 차이를 확인한다. 선택값이나 파일 이름이 다르다는 사실만으로 검수를 통과시키지 않는다.

## 5. 변환·대조·업로드·등록

`tools/upload-art.py`가 아래를 한 번에 한다. 손으로 할 때도 같은 순서다.

- 확정 결과를 **1024×1536 WebP**(quality 90)로 준비한다. 비율을 보존하며 임의 크롭·늘리기를 하지 않는다.
- 각성·저주를 기준컷과 **구도 대조**한다(흑백 96×144 정규화 내적). 0.6 이상이면 같은 원본, 0.3~0.6은 눈으로 본다 — 각성이 화면 전체에 빛을 뿌리면 배경 상관도가 떨어져 0.59가 나오기도 한다(아킬레우스의 창). 0.3 아래면 다른 구도이니 올리지 말고 사용자에게 묻는다.
- **명암**(흑백 표준편차)을 본다. 게임 키아트는 48~67이 보통. 벗어났다고 화풍이 틀린 건 아니다 — 금·흰옷·밝은 하늘이면 낮게 나온다(드라우프니르 43.8). 선화·셰이딩을 눈으로 본다.
- 파일 이름에는 생성에 실제 사용한 화풍 키를 넣는다. 카드 이름은 `card.json`의 정본이고 공백은 밑줄로. 오타가 흔하다(아른그레이프 → 야른그레이프) — 스크립트가 비슷한 이름을 댄다.
- `python3 tools/register-images.py --check`로 이름·화풍·슬롯 충돌을 본 뒤, 그림 저장소 `img/`에 **카드 하나에 커밋 하나**로 `main`에 직접 올린다. 작업 브랜치와 PR은 만들지 않는다. 같은 칸 이름으로 올리면 덮어쓴다.
- 올리면 자동이다: 그림 저장소의 썸네일 워크플로가 바뀐 파일만 다시 만들고 코드 저장소를 깨워 `data/img.json`에 적는다(보통 90초). `img/thumb/`와 `data/img.json`은 직접 커밋하지 않는다. 3분 넘게 안 되면 코드 저장소 Actions의 "그림 등록"을 수동 실행한다(`prune: "true"`는 옮기거나 지운 파일을 정리할 때).
- 반영 후 `data/img.json`에 카드·화풍·칸이 들어왔는지 한 번 확인하면 끝이다. 문제나 사용자 요청이 없으면 도감 화면 검수를 반복하지 않는다.
- 화풍을 옮길 때는 그림 저장소에서 `git mv`로 이름을 바꾸고 옛 썸네일을 지운 뒤, "그림 등록"을 `prune: "true"`로 돌린다.

## 6. 마지막 보고

생성 수/요청 수, 그림 저장소 반영 커밋, 구도 일치·명암, 카드의 `look`·`curse.text`와 맞는 점 한둘, 커버리지(`카드 n/40 — 북유럽 · 그리스 · 동아시아 · 인도`)만 짧게 보고한다. 정상 완료 시 검사 명령과 단계별 상태를 나열하지 않는다.

로컬 커밋만 만들었거나 푸시만 했다면 완료가 아니다. `data/img.json` 등록 자동화 성공 전에는 완료로 보고하지 않으며, 실제로 열어보지 못한 도감 화면까지 완료로 보고하지 않는다.
